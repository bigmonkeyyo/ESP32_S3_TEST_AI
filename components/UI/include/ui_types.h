#pragma once

typedef enum {
    UI_PAGE_CACHE_NONE = 0,
    UI_PAGE_CACHE_KEEP,
} ui_page_cache_mode_t;

typedef enum {
    UI_ANIM_NONE = 0,
    UI_ANIM_MOVE_LEFT,
    UI_ANIM_MOVE_RIGHT,
    UI_ANIM_FADE,
} ui_anim_t;
